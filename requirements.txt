# --- Web / API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
email-validator==2.2.0

# --- Auth ---
bcrypt==5.0.0
python-jose[cryptography]==3.3.0

# --- Data layer ---
SQLAlchemy==2.0.35

# --- RAG: vector store + embeddings + reranker ---
chromadb==0.5.5
sentence-transformers==3.1.1     # pulls torch; provides bge-m3 + bge-reranker

# --- LLM providers (install what you use) ---
openai==1.51.0
google-generativeai==0.8.3
# groq uses the openai client (base_url override) — no extra package needed
# cohere==5.11.0                 # uncomment if using Cohere rerank

# --- Eval / utils ---
PyYAML==6.0.2
numpy==2.1.1
tqdm==4.66.5

# --- Tests ---
pytest==8.3.3
